name: cap-reached-escalate
kind: skill
target: .
runner: dunning
inputs:
  invoice_status: overdue
  aging_days: 60
  cadence_policy:
    steps:
      - max_days: 7
        channel: email
      - max_days: 14
        channel: email
      - max_days: 30
        channel: letter
    cap: 3
expect:
  status: failure
  receipt:
    schema: runx.receipt.v1

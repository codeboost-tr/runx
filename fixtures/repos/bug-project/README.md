# Bug Project Fixture

Minimal fixture workspace for the runx scafld bug-to-PR chain.

The chain receives this path through the explicit `fixture` chain input and uses
it as the scafld working directory.

name: x402-pay-negative-authority-broader-child
kind: graph
target: ../graphs/payment/x402-pay-negative-authority-broader-child.yaml
caller:
  approvals:
    spend-approval: true
expect:
  status: failure
  steps:
    - quote
    - reserve
    - approve-spend

name: native-graph
lane: deterministic
target:
  kind: graph
  ref: ../../graphs/sequential/graph.yaml
expect:
  status: success
  output:
    subset:
      harness_id: hrn_sequential-echo_graph
      status: sealed
      disposition: closed
      step_count: 2

openapi: 3.1.0
info:
  title: Sourcey Fixture API
  version: 1.0.0
paths:
  /status:
    get:
      operationId: getStatus
      summary: Get fixture status
      responses:
        "200":
          description: Status response
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
